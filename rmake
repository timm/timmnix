Makefile = $(PWD)/rmake
Make     = $(MAKE) --no-print-directory 
Dirs     = $(shell find . -type d -print | grep -v \.git)

everywhere:
	@$(foreach Dir,$(Dirs), \
             (cd "$(Dir)"; \
	      $(MAKE) -f $(Makefile) Dir="$(Dir)" there);)

there:
	@echo $(Dir)
	@ls | wc -l
