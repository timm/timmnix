timmnix
=======

unix config utils

## Installation

```
cd $HOME
git clone 
cd timmnix
# this next step may take a while
bash install.sh USER EMAIL 

```

Optinally, add to `$HOME/.bash_profile`:

```
. tnix
```




